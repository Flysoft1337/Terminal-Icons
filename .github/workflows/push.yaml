name: CI
on: [push, pull_request]
jobs:
  test:
    name: test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macOS-latest]
    steps:
    - uses: actions/checkout@v3
    - name: Test
      run: pwsh -NoLogo -NoProfile -Command './build.ps1 -Task CompileTheme,Test -Bootstrap'
